# Supabase Email Templates

Paste these files into the matching Supabase Authentication email screens:

- `confirm-signup.html` → Confirm signup
- `reset-password.html` → Reset password
- `password-changed.html` → Password changed

Use the subjects listed in `subjects.txt`.

Do not alter `{{ .ConfirmationURL }}` in the signup or reset templates.
