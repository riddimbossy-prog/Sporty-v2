# v21.7.2 changes

- Preserves the custom SportyBet-backed API as the primary source.
- Adds fixture matching between verified code selections and `/api/get_upcoming_events`.
- Recovers missing kickoff dates only when the fixture match is sufficiently strong.
- Keeps unmatched selections undated instead of guessing.
- Disables API-Football fallback by default.
- Preserves the popular-tips board, animated prediction slip, practice points and branded sharing.
- Adds no real-money functionality.
