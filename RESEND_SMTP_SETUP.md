# Optional SMTP setup

Custom SMTP is optional and separate from the website deployment. Complete the Render and Supabase application checks first. If a custom SMTP provider is added later, store its password only in Supabase Authentication SMTP settings, verify the sending domain, and test signup and password-reset delivery before enabling it for all users.
