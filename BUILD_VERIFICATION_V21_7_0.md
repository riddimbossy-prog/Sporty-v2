# Build verification — v21.7.3

Validated locally:

- Full inherited test suite
- JavaScript syntax checks
- Custom API fixture-reconciliation regression test
- Custom SportyBet source priority
- API-Football fallback disabled by default
- Render build
- Local API smoke test
- PWA cache version and new matching runtime

Live upstream behavior must still be checked after deployment because public source responses can change.
