# Sporty.codes v21.7.3 — permanent Smart Board recovery

- Enriches verified Code Hub selections with kickoff dates from the custom SportyBet upcoming-events feed before Supabase storage.
- Persists the recovered kickoff, league and event id so every client receives the same date.
- Keeps strict fixture-name confidence matching; unmatched selections are never guessed.
- When the upstream event feed has no date, Smart Board renders repeated selections under a clear `Date pending` fallback instead of appearing empty.
- No real-money features and no API-Football replacement.
